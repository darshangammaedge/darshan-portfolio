# stages:
#   - deploy_infra
#   - deploy_site

# variables:
#   STACK_NAME: "MyPrivateStaticSite"
#   # Name of your existing KeyPair in AWS Console
#   KEY_NAME: "your-aws-key-pair"
#   # This constructs the private URL using your Token
#   PRIVATE_REPO_URL: "https://oauth2:${GITLAB_TOKEN}@://gitlab.com{CI_PROJECT_PATH}.git"

# infrastructure:
#   stage: deploy_infra
#   image: 
#     name: amazon/aws-cli:latest
#     entrypoint: [""]
#   script:
#     - echo "Deploying CloudFormation..."
#     - |
#       aws cloudformation deploy \
#         --stack-name $STACK_NAME \
#         --template-file template.yaml \
#         --parameter-overrides KeyName=$KEY_NAME GitHubRepoURL=$PRIVATE_REPO_URL \
#         --capabilities CAPABILITY_IAM
#     # Get Instance IP
#     - export INSTANCE_IP=$(aws ec2 describe-instances --filters "Name=tag:aws:cloudformation:stack-name,Values=$STACK_NAME" "Name=instance-state-name,Values=running" --query "Reservations[*].Instances[*].PublicIpAddress" --output text)
#     - echo "INSTANCE_IP=$INSTANCE_IP" >> deploy.env
#   artifacts:
#     reports:
#       dotenv: deploy.env

# update_site:
#   stage: deploy_site
#   image: alpine:latest
#   before_script:
#     - apk add --no-cache openssh-client
#     - chmod 400 $SSH_PRIVATE_KEY
#   script:
#     - echo "Updating private code on $INSTANCE_IP..."
#     # The 'EOF' block runs these commands inside the EC2 instance
#     - |
#       ssh -o StrictHostKeyChecking=no -i $SSH_PRIVATE_KEY ec2-user@$INSTANCE_IP << EOF
#         cd /var/www/html
#         # If folder is empty, clone it. Otherwise, pull.
#         if [ -d ".git" ]; then
#           sudo git pull origin main
#         else
#           sudo rm -rf *
#           sudo git clone ${PRIVATE_REPO_URL} .
#         fi
#         sudo chown -R ec2-user:apache /var/www/html
#         sudo systemctl restart httpd
#       EOF
#   only:
#     - main


name: Push-to-Deploy Pipeline

on:
  push:
    branches: [ main ] # Change to 'master' if your branch name is different

env:
  STACK_NAME: "MyStaticSiteStack"
  AWS_REGION: "ap-south-1"
  KEY_NAME: "deploy-key"
  REPO_URL: "https://github.com{{ github.repository }}.git"

jobs:
  sync-infra-and-code:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy/Update CloudFormation
        uses: aws-actions/aws-cloudformation-github-deploy@v2
        with:
          name: ${{ env.STACK_NAME }}
          template: template.yaml
          parameter-overrides: "KeyName=${{ env.KEY_NAME }},GitHubRepoURL=${{ env.REPO_URL }}"
          no-fail-on-empty-changeset: "1"

      - name: Get Instance IP
        id: get-ip
        run: |
          IP=$(aws ec2 describe-instances --filters "Name=tag:aws:cloudformation:stack-name,Values=${{ env.STACK_NAME }}" "Name=instance-state-name,Values=running" --query "Reservations[*].Instances[*].PublicIpAddress" --output text)
          echo "ip=$IP" >> $GITHUB_OUTPUT

      - name: Update Web Files via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ steps.get-ip.outputs.ip }}
          username: ec2-user
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/html
            sudo git fetch --all
            sudo git reset --hard origin/main
            sudo chown -R ec2-user:apache /var/www/html
